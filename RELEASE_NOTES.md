# RELEASE NOTES

## 0.3.0-rc.1

### 版本概览

本次发布是一次功能增强版本，主要围绕**点名体验优化**、**数据导入重构**和**出勤状态管理升级**三大方向。表格排序、自动滚动、重复点名控制等功能让点名流程更加流畅；全新的三步导入向导大幅降低了批量导入学生的门槛；出勤状态从硬编码升级为数据库管理，为后续自定义状态类型打下基础。

### 主要特性

#### 1. 表格排序功能
- 所有列表页面（学生管理、点名、历史记录）的表格现在支持**单击表头排序**。点击姓名、学号、时间等列头即可按该字段升序/降序排列，中文姓名按拼音排序，排序方向有清晰的图标指示。
- 点名页面默认**按照点名时间升序排列**，排序方式会展示到表头上。
- 历史记录页面默认**按照数据库插入顺序倒序排列**，但受限于依赖本地时间，不能完全保证按照时间倒序排列，故排序方式不会体现在表头上。

#### 2. 点名体验升级
- **自动滚动**：点名时表格自动滚动到底部，无需手动滚动即可看到最新点到的学生
- **重复点名控制**：新增“允许重复点名”开关，关闭后所有学生被点完时列表自动清空重置
- **分组显示优化**：仅在按点名时间排序时显示分组色块，其他排序方式下自动隐藏，避免干扰

#### 3. 学生导入全面重构
导入流程升级为**三步向导**，每一步都清晰指引：
- **步骤一**：选择 Excel 文件
- **步骤二**：配置列映射（姓名/学号对应哪一列），支持拖拽箭头调整表头行数
- **步骤三**：导入预览与冲突处理，决策完成后一键导入

新界面更简洁，误操作更少，冲突处理更直观。

#### 4. 出勤状态数据库化
出勤状态（出勤/缺勤/请假等）现在由数据库统一管理，不再依赖前端硬编码。这意味着未来可以**自定义出勤状态类型和颜色**（该功能将在后续版本开放），为不同场景提供更灵活的考勤配置。

#### 5. UI 细节打磨
- 单选/复选框悬停时鼠标变为手型，提示可点击
- 卡片弹窗最大宽高限制，避免在大屏上过度拉伸
- 表头文字不可选中，避免误操作时意外选中文字

### 问题修复

- 修复 CI 环境（持续集成）中版本号显示异常的问题
- 修复特定场景下点名器获取空学生 ID 导致服务崩溃的问题
- 修复编辑考勤记录时未选择任何修改项但“确定”按钮仍然可点的问题
- 修复点名后学生状态错误显示为“缺勤”的问题
- 修复非时间排序时表格仍然自动滚动到底部的干扰行为

### 升级注意

1. **数据库迁移**：本次版本包含数据库结构变更，启动时会自动执行迁移。由于当前尚处于早期开发阶段，我们不能确保完全兼容旧数据，**请务必在升级前备份数据库**。
2. **前端兼容性**：如果使用了自定义脚本直接调用出勤状态相关接口，请注意状态 ID 已从 0 起始调整为 1 起始（0 为回退值），具体请参考[变更日志](CHANGELOG.md)。
3. **旧导入组件已移除**：先前版本的三步导入图标入口已统一指向新导入组件，功能无损失，但操作路径略有变化。

### 反馈建议

如有问题或建议，欢迎在 [Issue](https://github.com/bytecascade-Lee/rollcaller/issues) 区提出。

## 0.2.0

### 导出功能全面升级

- **数据导出**：
  - 新增历史记录导出，同时支持三种导出方式——导出全部记录、导出当前筛选后的记录、或仅导出选中的记录。
  - 导出时系统会弹出保存窗口，可以选择保存位置和文件名，默认文件名会自动添加时间戳，避免覆盖之前的文件。
  - 表格列宽会根据内容自动适配，导出的 Excel 文件更加整洁易读。
- **导出状态提示**：点击导出后，界面会显示“导出中”、“导出成功”或“导出失败”的状态，展示操作进展。

### 交互体验优化

- **点击外部关闭弹窗**：现在打开部分气泡卡片弹窗（如编辑、导出等）后，点击弹窗外部区域即可自动关闭，操作更加顺手。
- **弹窗定位更智能**：弹窗会尽量显示在可视区域内，避免被屏幕边缘遮挡。
- **窗口居中显示**：应用启动时主窗口自动居中，无需手动调整。

### 界面与样式更新

- **更清晰的文本层级**：标题、副标题、正文内容使用统一的样式规范，信息层次更分明。
- **卡片式布局**：部分功能模块采用卡片样式，视觉更清爽，交互反馈更明显。
- **表单输入优化**：输入框、复选框、单选框样式更新，焦点高亮更醒目，禁用状态显示更清晰。
- **按钮防误触**：按钮和工具栏文本不再被鼠标意外选中，操作更精准。

### 其他改进

- 点名状态编辑页面现在支持“选中/未选中”两种显示样式，状态区分更直观。
- 编辑学生信息时，若学号冲突会有清晰提示，保存按钮在无效状态下自动禁用，避免误操作。

### 问题修复

- 修复了点名结果展示区域中第一次点名时无法自动滚动的问题。
- 修复了记录历史页面点击“刷新”按钮偶尔无响应的问题。
- 修复了导出文件时 Windows 系统下确认覆盖已有文件时失败的问题。
- 修复了部分按钮在禁用状态下仍显示为可点击样式的问题。

### 反馈与建议

如有问题或建议，欢迎在 [Issue](https://github.com/bytecascade-Lee/rollcaller/issues) 区提出。

## 0.1.0

### 关于本版本

这是「点名系统」的第一个正式发布版本，提供了较为完善的学生管理、点名记录和统计查看功能。

### 主要功能

- **点名**：从学生列表中随机抽取姓名，动画流畅，结果清晰展示
- **学生管理**：支持添加、编辑、删除学生信息（学号、姓名）
- **批量导入/导出**：通过 Excel 文件批量导入学生名单，支持导出数据
- **记录历史**：查看所有点名记录，支持按学号/姓名搜索筛选
- **状态标记**：每条记录可标记出勤状态（如已到、未到等）

### 使用须知

- 本版本仅提供 Windows 平台安装包（Setup 安装包 + 便携版 Zip）
- 数据本地存储，无需联网

### 已知问题

- 气泡卡片不支持点击外部区域关闭
- 部分页面/卡片配色不统一

### 反馈与支持

如有问题或建议，欢迎在 [Issue](https://github.com/bytecascade-Lee/rollcaller/issues) 区提出。

## 0.1.0-rc.1
> 一款轻量、好用的课堂自动点名工具

### 它能做什么？

**点名**
- 随机抽取学生名单，支持**单次点名**和**连续点名**两种模式
- 点名记录自动保存，按**点名轮次**分组展示，一目了然

**学生管理**
- 增删改查，支持**软删除**和**恢复**
- 支持按学号/姓名**快速搜索**
- **批量删除**和**批量恢复**，管理更高效

**考勤记录**
- 查看完整的历史记录列表
- 支持**批量修改**考勤状态（缺勤 / 出勤 / 迟到 / 早退 / 请假）
- 可批量添加或修改备注信息

**数据导入导出**
- 支持从 **Excel** 导入学生名单，可配置列映射，自动处理重复数据
- 支持导出学生列表到 **Excel** 文件

### 系统支持

- Windows NSIS 安装包
- Windows 便携模式

### 安装方式

1. 下载对应平台的安装包或便携版压缩包
2. 安装包：运行安装程序，按提示完成安装
3. 便携版：解压后直接运行 `rollcaller.exe`

### 反馈与建议

如有问题或建议，欢迎在 [Issue](https://github.com/bytecascade-Lee/rollcaller/issues) 区提出。
