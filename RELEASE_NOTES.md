# 版本发布说明

> 如有问题或建议，欢迎在 [Issue](https://github.com/bytecascade-Lee/rollcaller/issues) 区提出。

---

## 0.3.0

### 版本亮点

全面升级点名与导入体验：表格排序让数据浏览更高效，全新三步导入向导大幅降低批量导入门槛。

### 新增功能

- **表格排序**：学生管理、点名、历史记录三个页面的表格均支持单击表头排序，支持中文姓名排序，排序方向有清晰图标指示。
- **点名体验升级**
  - 点名后表格自动滚动到底部，无需手动翻看。
  - 新增"允许重复点名"开关——关闭后，所有学生被点完时列表自动清空重置。
  - 分组色块仅在按时间排序时显示，其他排序方式下自动隐藏。
- **全新导入向导**：导入流程升级为三步向导（选择文件 → 配置列映射 → 导入与冲突处理），操作更直观，误操作更少。

### 体验改进

- 出勤状态（出勤/缺勤/请假等）改为数据库统一管理，为后续自定义状态类型打下基础。
- 单选/复选框悬停时鼠标变为手型，提示可点击。
- 弹窗最大宽高限制，避免在大屏上过度拉伸。
- 表头文字不可选中，避免误操作时意外选中文字。
- 应用页脚版本号现在动态显示实际版本，不再固定显示旧版本号。
- 空状态页面提示更简洁。

### 重要修复

- 修复点名时在特定情况下可能崩溃的问题。
- 修复点到的学生状态错误显示为"缺勤"的问题。
- 修复非时间排序时表格仍然自动滚动到底部的干扰行为。
- 修复编辑考勤记录时未做任何修改但"确定"按钮仍然可点的问题。

### 升级注意

1. **请在升级前备份数据库。** 本次包含数据库结构变更，启动时会自动迁移。
2. 出勤状态内部编号有调整，如果使用了自定义脚本调用相关接口，请注意适配。
3. 旧版导入入口已统一指向新版导入向导，功能无损失，但操作路径略有变化。

### 已知问题

暂无。

---

## 0.3.0-rc.2

本版本包含 0.3.0-rc.1 的全部内容，以下为增量变更：主要打磨界面细节、修正版本号显示问题。

- **修复**：应用页脚版本号之前固定显示旧版本号，现已修正为动态显示当前实际版本。
- **改进**：空状态页面提示文字精简，界面更清爽。

本次升级无数据库迁移，可直接替换安装。

---

## 0.3.0-rc.1

第一个候选发布版本，围绕点名体验优化、导入重构和出勤状态管理三大方向。

- **新增**：所有列表表格支持单击表头排序；点名后自动滚动、新增重复点名控制开关；导入流程重构为三步向导；出勤状态改为数据库管理。
- **改进**：复选框/单选框悬停手型、弹窗尺寸限制、表头文字不可选中。
- **修复**：点名时可能崩溃、状态显示错误、未修改时确定按钮可点、非时间排序时仍滚动等问题。

> ⚠️ 本次包含数据库变更，升级前请备份。出勤状态内部编号有调整，详见 [CHANGELOG](CHANGELOG.md)。

---

## 0.2.0

### 版本亮点

导出功能全面升级，支持三种导出模式和导出状态反馈；交互体验显著改善，弹窗关闭、定位、窗口居中等细节全面优化。

### 新增功能

- **数据导出**：
  - 历史记录支持三种导出方式——全部记录、当前筛选后的记录、仅选中的记录。
  - 导出时弹出保存窗口，可选择保存位置和文件名，默认文件名自动添加时间戳，避免覆盖。
  - 表格列宽根据内容自动适配，导出的 Excel 文件更整洁易读。
- **导出状态提示**：点击导出后，界面实时显示"导出中""导出成功"或"导出失败"。
- **窗口居中**：应用启动时主窗口自动居中显示。

### 体验改进

- **点击外部关闭弹窗**：打开弹窗后点击外部区域即可自动关闭。
- **弹窗定位更智能**：弹窗会尽量显示在可视区域内，避免被屏幕边缘遮挡。
- **更清晰的文本层级**：标题、副标题、正文使用统一的样式规范，信息层次更分明。
- **卡片式布局**：部分功能模块采用卡片样式，视觉更清爽。
- **表单元素更新**：输入框、复选框、单选框样式刷新，焦点高亮更醒目，禁用状态显示更清晰。
- **按钮防误触**：按钮和工具栏文本不再被鼠标意外选中。

### 重要修复

- 修复第一次点名时结果区域无法自动滚动的问题。
- 修复历史记录页面点击"刷新"按钮偶尔无响应的问题。
- 修复导出文件时 Windows 系统下确认覆盖已有文件失败的问题。
- 修复部分按钮在禁用状态下仍显示为可点击样式的问题。

### 升级注意

无破坏性变更，可直接替换升级。

### 已知问题

暂无。

---

## 0.1.0

### 版本亮点

「点名系统」首个正式版本，覆盖从点名、学生管理到考勤记录的完整工作流，目前仅支持 Windows 平台，数据本地存储无需联网。

### 新增功能

- **随机点名**：从学生列表中随机抽取，支持单次和连续点名两种模式，动画流畅，结果清晰展示。
- **学生管理**：支持添加、编辑、删除学生信息，支持按学号/姓名搜索。
- **批量导入**：通过 Excel 文件批量导入学生名单，支持列映射配置和重复数据处理。
- **数据导出**：支持将学生列表导出为 Excel 文件。
- **考勤记录**：查看所有点名记录，支持按学号/姓名筛选搜索，可批量修改考勤状态和备注。
- **记录分组**：考勤记录按点名轮次自动分组展示，一目了然。

### 使用须知

- 本版本仅提供 Windows 平台安装包（Setup 安装包 + 便携版 Zip）。
- 数据本地存储，无需联网。

### 已知问题

- 弹窗不支持点击外部区域关闭。
- 部分页面/卡片配色不统一。

---

## 0.1.0-rc.1

首个候选发布版本，奠定了点名、学生管理、考勤记录和数据导入导出等核心功能，内容已并入 0.1.0 正式版。详见上方 0.1.0。
