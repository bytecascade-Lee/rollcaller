# CHANGELOG

## 0.3.0

### 界面

- 调整部分文字说明

---

## 0.3.0-rc.2

### 新增

- **版本号统一同步脚本**：新增 `sync-version.py` 脚本，支持一键同步 `pyproject.toml`、`tauri.conf.json5`、`Cargo.toml`、
  `package.json` 中的版本号
    - 自动去除版本号前导 `v` 前缀并校验语义化版本格式
    - 支持语义化版本（含 `alpha`/`beta`/`rc` 等预发布标识）
    - 修改后自动执行 `uv lock` 同步锁文件
    - 采用行匹配替换，无需第三方依赖

### 修复

- **CI 构建**：修复构建信息获取不到版本号的问题，确保 `VERSION` 环境变量正确传递
- **构建信息生成**：从 `VERSION` 环境变量读取版本号，缺省时使用 `CARGO_PKG_VERSION`，并自动去除 `v`/`V` 前缀
- **构建时间格式**：调整 `BUILD_TIME` 格式化方式，去除时区括号部分，避免产生多余信息
- **应用信息展示**：页脚版本号改用动态获取的 `APP_INFO.version`，替代硬编码的 `v0.1.0`
- **展示格式优化**：`commit_time` 与 `build_time` 分隔符统一改为 `#`

### 重构

- **应用信息实体类**：新增 `version` 字段，初始化时自动从环境变量获取
- **应用信息实体类型**：同步新增 `version` 字段定义

### 依赖与工具链

- 添加 ESLint 及 Svelte 相关开发依赖（`eslint`、`eslint-plugin-svelte` 等）
- 初始化 ESLint 配置文件 `eslint.config.js`，支持 Svelte + TypeScript，关闭格式化规则保留代码质量检查
- 将 `@eslint/js` 从 `dependencies` 移至 `devDependencies`

### 界面

- 简化空状态提示文案：将“当前还未点名，暂无记录，请先点名”改为“当前暂无记录”

---

## 0.3.0-rc.1

### 破坏性变更 (Breaking Changes)

- 出勤状态从硬编码常量迁移至数据库存储，`AttendanceStatusBadge` 组件的 `code` 属性改为 `id`，前端调用需同步更新 (#34, #36, #41,
  #45, #48, #49)
- 移除 `constants/AttendanceStatus.ts`，改为 `types/AttendanceStatus.ts` 导出 (#41)
- `Result` 枚举类扩展：新增 `Info`，`Warn` 重命名为 `Warning`，影响消息处理逻辑 (#22, #23)

### 新增

#### 表格排序

- 学生管理、点名、历史记录三个页面的表格支持表头单击排序，支持中文拼音排序，排序方向通过图标指示 (#05, #06, #07)
- 抽取排序 key 为共享常量，减少重复代码 (#08)
- 新增 `ArrowsDownUpIcon` 图标提示可排序列 (#11, #12, #13)

#### 点名功能

- 新增“允许重复点名”开关，不重复模式下全部点完后自动清空列表 (#25, #26)
- 默认按点名时间升序排列时，点名后表格自动滚动到底部，保持视觉跟踪 (#29, #30)
- 仅按点名时间排序时显示分组色块，切换其他排序键时自动隐藏 (#52)

#### 学生导入重构

- 导入弹窗重构为三步向导：选择文件 → 配置列映射 → 导入与冲突处理 (#20)
- 表头行数调整为表格左侧冻结列内的拖拽箭头，指向某行即忽略该行及之前所有行 (#20)
- 新增 `card-group` 类，支持横向分散排列 (#17)
- 删除旧导入组件，统一使用新组件入口 (#19)

#### 出勤状态数据库化

- 新增 V20、V30 数据库迁移，创建 `attendance_status_definition` 表并迁移历史数据 (#34, #35)
- 新增出勤状态实体类、Repository、Service、Command 全套后端实现 (#36, #37, #38, #39)
- 注册三个前端接口：创建、更新、获取全部状态 (#40)
- 新增 `AttendanceStatusStore.svelte.ts` 管理全局状态 (#44)
- `EditRecord` 组件改用 store 获取状态列表，过滤 id=0 的缺省状态 (#46)

#### UI/样式增强

- 单选框、复选框鼠标手势改为 `pointer` (#04)
- 卡片/浮窗最大宽度限制为 85%，最大高度为 80% (#15)
- 新增 `Switch` 开关通用组件 (#26)
- 提取 switch 样式到共享 CSS 文件 (#27)
- 新增 `budge-button` 类，包围budge的按钮不再依赖 `budge` 类 (#24)

### 修复

- 修复 CI 中分支名展示为 "HEAD" 和提交数为 1 的问题 (#01)
- 修复表头文字可被选取的问题，鼠标手势改为 `pointer` (#03)
- 修复点名器在动态学生列表下可能获取空 ID 导致后端 panic 的问题 (#31)
- 修复 `EditRecord` 未选择任何更新项时“确定”按钮仍启用的问题 (#47)
- 修复点到的学生状态显示为“缺勤”的问题（状态值统一 +1） (#51)
- 修复按照非时间排序时仍然滚动到底部的问题 (#30)
- 修复对 type 类型的异常导入，使用 `import type` 导入类型 (#43)

### 重构与优化

- 重构历史记录页面的过滤逻辑，降低代码复杂度 (#09)
- 重构点名页面的新数组创建逻辑，由 `filter` 改为 `[...records]` 避免意外修改原数组 (#10)
- 重构点名页面默认排序实现，不再通过 `reverse` 翻转，改用点名时间排序 (#14)
- 重构新版导入学生组件的布局：外部遮罩不可点击、移除右上角关闭按钮、调整标题样式、删除冗余 CSS (#16, #18)
- 统一分布导入组件的 CSS 样式 (#18)
- 修复在按照其他方式排序时仍然滚动到底部的问题 (#30)
- 出勤状态相关组件全面改用 store 获取数据 (#45, #48, #49)

---

## 0.2.0

### 新增

- **前端交互**
    - 新增 `clickOutside` 动作，支持点击外部关闭弹窗，可配置排除元素 (#02, #03)
    - 新增 `updatePosition` 通用定位函数，支持视口边界检测与自适应 (#17)
    - 新增 `Result` 枚举类型（Doing/Success/Warn/Error/None） (#22)

- **数据导出**
    - 导出文件名添加时间戳避免覆盖 (#30)
    - 导出功能增加状态反馈（进行中/成功/失败） (#30, #48)
    - 后端导出增加列宽自适应算法（后改为固定宽度）及居中对齐格式 (#43, #44, #45)
    - 记录导出支持全部、筛选后、选中三种模式，集成 Tauri 文件保存对话框 (#48, #49)

- **组件增强**
    - `AttendanceStatusBadge` 增加 `selected` 属性，支持选中/未选中样式切换 (#12)
    - 编辑记录组件中，对于当前选择数量为1时，自动填充原始数据 (#16)
    - 学生导入组件新增分步骤版本（当前隐藏，后期优化后代替现有导出组件） (#67)

- **构建与脚本**
    - 初始化 `uv` 配置文件 `pyproject.toml` 及锁文件 (#36, #37)
    - 变更日志生成脚本支持 `-s/-e/-a` 范围参数及 commit hash 边界 (#69)
    - 生成的文档头部添加当前分支名 (#70)

- **窗口与体验**
    - 窗口启动时居中显示 (#38)

### 样式重构

- 弹窗样式文件 `dialog.css` 重命名为 `popup.css`，调整间距与最小宽度 (#04, #58)
- 边框颜色变量层级整体迁移（`--gray-3` -> `--gray-10` 映射调整） (#07)
- 表单元素优化：复选框/单选框主色调、输入框大圆角、焦点高亮、禁用状态光标 (#09)
- 考勤状态徽章增加分组、悬浮、选中反色滤镜效果 (#11)
- 移除按钮 `selected` 边框样式、弹窗标题样式、部分无用 CSS (#10, #29, #64)
- 新增文本样式组件（`text.css`），支持标题/副标题/内容及状态颜色变体 (#24)
- 新增卡片组件样式（`card.css`）及背景色变量 `--color-card` (#25, #26)
- 按钮、输入框占位符、工具栏等增加 `user-select: none` 防止误选 (#27, #33)

### 问题修复

- 修复图标按钮禁用状态下仍显示激活背景色的问题 (#28)
- 修复后端服务层考勤状态码映射错误 (#41)
- 修复导出时文件存在检查导致 Windows 原生保存对话框无法覆盖的问题 (#42)
- 修复记录历史页面刷新按钮因 `this` 上下文丢失导致无法访问私有字段 `#isLoading` 的问题 (#53)
- 修复点名结果展示区域第一次点名时不滚动的问题 (#61)
- 修复变更日志脚本中 commit 长度计算错误及 `-e` 单参数无法获取提交内容的问题 (#68, #71)

### 代码优化与重构

- 移除后端中不必要的返回值处理（`student_batch_delete`、`student_export`） (#23)
- 数据库迁移服务日志从 `println!` 升级为 `info!`/`debug!` 宏 (#51)
- 格式化记录存储类代码，统一缩进 (#54)
- 移除前端多处调试日志及冗余点击事件 (#20, #57)
- 点名页面重构工具栏结构，统一按钮样式类 (#35)
- 编辑记录组件代码组织优化，函数顺序调整 (#16)

---

## 0.1.0

### 🚀 新增

- **UI 设计系统**：建立完整的 CSS 变量体系，涵盖颜色、间距、字体、圆角、阴影、过渡等设计令牌
- **布局组件**：新增侧边栏、导航栏、工具栏、页脚、标题栏等布局样式组件
- **表单组件**：实现输入框、选择器、文本域等表单元素样式及全局重置
- **数据展示**：新增表格组件，支持固定表头、行悬停、自定义滚动条
- **反馈组件**：新增弹窗（Dialog/Popup）、覆盖层（Overlay）、状态徽章（Badge）等交互组件
- **按钮体系**：实现图标按钮、文字按钮及按钮组，支持 yes/warn/error 多种类型
- **搜索组件**：统一搜索框样式，支持焦点状态交互
- **点名引擎**：实现点名核心逻辑，动画间隔 120ms，持续时间 720ms，结果展示 1200ms
- **应用信息**：构建时自动注入 Git 分支、提交次数、提交哈希、构建时间到前端
- **变更日志生成**：内置脚本自动生成 Markdown 格式的提交记录变更日志

### ✨ 优化改进

- 统一 CSS 类名命名规范，提升语义化
- 优化中文字体显示，默认字体切换为 "Microsoft Yahei"
- 表格结果面板高度比例调整为 20%
- 按钮组布局从 `inline-flex` 改为 `flex`，支持右对齐
- 图标按钮使用 `aspect-ratio: 1` 保持正方形比例

### 🐛 修复

- 修复点名页面无记录时布局跳动问题
- 修复学生编辑功能中更新数据不一致的问题（使用数据库返回的最新数据）
- 修复历史记录页面搜索图标大小与其他页面不一致的问题
- 修复禁用状态颜色变量拼写错误（`diasbled` → `disabled`）
- 修复导入学生组件中 ImportCommand 路径错误
- 修复应用信息命令中调试宏调用错误

### 🛠️ 技术债务

- 移除全局样式中的冗余代码，将样式拆分为独立模块文件
- 移除内联样式，统一使用 CSS 类
- 移除废弃的 overlay、dialog、field 相关样式规则
- 重构模块导入顺序，按字母排列提升可读性

---

## 0.1.0-rc.1

### 🚀 新增功能

- **核心逻辑**
    - 随机点名功能，支持 **单次点名**与 **连续点名**两种模式
    - 点名流程由状态机驱动（空闲 → 动画 → 选人 → 展示），交互流畅
    - 点名结果自动写入数据库，生成考勤记录，并按**会话（session）**分组展示

- **学生管理**
    - 完整 CRUD 操作：新增、编辑、删除、批量删除
    - 支持 **搜索**学生（学号 / 姓名）
    - 批量操作删除便捷高效

- **考勤记录管理**
    - 记录列表展示，按点名会话自动分组，视觉区分清晰
    - 支持 **批量修改**选定记录的考勤状态（缺勤 / 出勤 / 迟到 / 早退 / 请假）和备注
    - 记录详情中显示关联的学生信息（学号、姓名）

- **导入导出**
    - **Excel 导入**：
        - 支持选择 Excel 文件，预览前几行数据
        - 可配置 **列映射**（学号列、姓名列）和 **表头行数**
        - 自动检测数据冲突（学号重复），提供 **覆盖 / 跳过 / 保留**决策选项
    - **Excel 导出**：
        - 导出学生列表（全部或选中）

- **应用模式与路径管理**
    - 支持 **安装模式**和 **便携模式**（通过 `portable.mode` 文件检测）
    - 自动适配不同模式下的数据、配置、缓存、日志目录
    - 单实例运行

- **数据库与迁移**
    - 内置 **SQLite** 数据库，自动创建及初始化
    - 迁移机制支持版本化表结构升级，记录执行历史
    - 迁移文件自动加载并校验哈希，保证迁移一致性

- **UI/UX**
    - 基于 **Svelte 5** + **TypeScript** 构建，响应式交互
    - 侧边栏导航，三核心页面：点名、记录历史、学生管理
    - 统一弹窗管理，各对话框集中控制
    - 使用 **Phosphor** 图标库和 **Open Props** 设计令牌，提升视觉表现

- **开发体验**
    - 提供跨平台开发启动脚本（Windows、macOS/Linux）
    - Rust 后端分层架构（cmd / service / repo），职责清晰
    - 前端路径别名（`$components`、`$stores`、`$utils` 等）简化导入
    - 支持调试模式下查看配置键名文件，便于开发调优

### ✨ 优化改进

- **后端**
    - 使用 `anyhow` 统一错误上下文，提高调试信息友好度
    - 数据库连接池采用懒加载单例，支持开发/生产环境独立数据库文件
    - 日志系统支持控制台、文件两种输出
    - 路径管理模块支持多模式（开发/便携/安装），资源目录定位更健壮

- **前端**
    - 状态管理重构为 **Store 类**（`StudentStore`、`RecordStore`），逻辑集中，可维护性提升
    - 命令调用统一封装（`StudentCommand`、`RecordCommand`、`ImportCommand`、`RollcallCommand`），替代直接 `invoke`
    - 组件样式统一迁移到 **CSS 变量驱动**的设计系统，移除硬编码，便于主题扩展
    - 各页面组件结构调整，支持 `active` 属性控制激活状态，性能优化

- **通用**
    - 重命名多个命令函数，提高命名一致性和可读性
    - 合并冗余的批量更新接口（状态+备注合并为单一批量更新）
    - 优化事务处理，确保数据一致性（如学生创建、记录插入等）
    - 增强错误提示，用户界面中的错误信息更加明确

### 🐛 修复

- 修复学生更新时学号冲突检测逻辑，允许更新 **自身**学号（避免误判冲突）
- 修复记录创建后 ID 获取失败的问题，确保新记录正确返回给前端
- 修正学生批量删除和恢复 SQL 条件中的字段名错误（`ids` → `id`）
- 修复事务提交后继续查询导致的错误（调整事务内操作顺序）
- 修复 Windows 平台便携模式和安装模式下资源路径计算异常
- 修复前端学生编辑组件中多选状态下的空值处理及数据绑定问题
- 修复导入组件中 `Map` 类型操作错误（使用 `get/set/delete` 替代索引访问）
- 修复历史记录页面自动重复加载数据的问题
- 修复学生创建对话框遮罩点击意外关闭的问题
- 修复开发环境下 JSON 日志配置导致的构建失败
